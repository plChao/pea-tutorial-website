關於 `input()` 和 `input("請輸入：")` 的差別，下面哪一個敘述是對的？

<select1>A. `input("請輸入：")` 會在等待輸入之前，先顯示「請輸入：」這段提示文字</select1>
<select2>B. `input("請輸入：")` 會把「請輸入：」當成輸入的內容</select2>
<select3>C. 兩者完全沒有差別</select3>
<select4>D. `input("請輸入：")` 執行會出現錯誤</select4>
<ans>A</ans>
<detail>
`input()` 括號裡可以放一段字串當作提示文字，執行到這行時會先把提示文字顯示出來，再暫停等待使用者輸入，方便使用者知道現在該輸入什麼。
</detail>
