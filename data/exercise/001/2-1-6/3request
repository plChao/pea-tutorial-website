下面哪一組型別分類是正確的？

<select1>A. 可變(mutable)：str、list；不可變(immutable)：int、dict</select1>
<select2>B. 可變(mutable)：list、dict；不可變(immutable)：int、str</select2>
<select3>C. 可變(mutable)：int、float；不可變(immutable)：list、dict</select3>
<select4>D. 全部型別都是可變的</select4>
<ans>B</ans>
<detail>
`list` 和 `dict` 建立之後可以直接修改內容(原地修改)，是可變的；`int`、`float`、`str`、`bool` 建立之後不能修改，任何看起來像「修改」的操作都是建立一個新物件，是不可變的。
</detail>
