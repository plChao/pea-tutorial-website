請寫一段程式(不需要讀任何輸入)，依序完成下面的步驟並印出結果：

1. 建立 `a = [1, 2, 3]`
2. 建立 `b = a`(注意：這樣寫不會複製)
3. 印出 `a is b`
4. 用 `b.append(4)` 修改 `b`
5. 印出 `a`(驗證 `a` 有沒有被影響到)
6. 建立 `c = a[:]`(這次是真正複製一份)
7. 印出 `a is c`
8. 用 `c.append(99)` 修改 `c`
9. 印出 `a`(驗證這次 `a` 有沒有被影響到)

輸出應該是：

<pre><code>True
[1, 2, 3, 4]
False
[1, 2, 3, 4]</code></pre>
