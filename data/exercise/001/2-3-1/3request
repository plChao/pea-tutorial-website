下面四個運算子，哪一個的計算結果**一定**是 `float`？

<select1>A. //</select1>
<select2>B. %</select2>
<select3>C. /</select3>
<select4>D. *</select4>
<ans>C</ans>
<detail>
`/`(一般除法)不管兩個運算元是不是能整除，結果一律是 `float`，例如 `6 / 2` 會得到 `3.0`。

`//`、`%`、`*` 在兩個運算元都是 `int` 的時候，結果都會是 `int`。
</detail>
