執行下面這段程式：

<pre><code>print(2 + 3 * 4)</code></pre>

會印出什麼？

<select1>A. 20</select1>
<select2>B. 14</select2>
<select3>C. 24</select3>
<select4>D. 9</select4>
<ans>B</ans>
<detail>
乘法 `*` 的優先順序比加法 `+` 高，所以會先算 `3 * 4 = 12`，再算 `2 + 12 = 14`。

如果想要先算 `2 + 3`，要寫成 `(2 + 3) * 4`，這樣才會得到 `20`。
</detail>
