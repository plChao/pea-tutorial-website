執行下面這段程式：

<pre><code>print(7 // 2)</code></pre>

會印出什麼？

<select1>A. 3</select1>
<select2>B. 3.5</select2>
<select3>C. 3.0</select3>
<select4>D. 4</select4>
<ans>A</ans>
<detail>
`//` 是整數除法，只留下商、無條件捨去小數部分。7 除以 2 商 3 餘 1，所以 `7 // 2` 是 `3`(int)。

如果寫的是 `7 / 2`，結果才會是 `3.5`(float)。
</detail>
