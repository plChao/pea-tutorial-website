第一行讀入兩個整數 r c，代表要建立一個 r×c、每格都是 0 的網格(用 `[[0] * c for _ in range(r)]` 建立)。

第二行讀入一個整數 k，接下來有 k 行，每行三個整數 `row col value`，代表把網格中第 row 列、第 col 行的值改成 value(索引從 0 開始)。

全部更新完後，依照每一列輸出，同一列的數字之間用空白隔開。

範例：如果輸入是

<pre><code>2 3
2
0 1 5
1 2 9</code></pre>

輸出應該是：

<pre><code>0 5 0
0 0 9</code></pre>
