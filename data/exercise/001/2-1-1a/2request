執行以下程式：

<pre><code>print("A")
print()
print("B")</code></pre>

輸出總共會有幾行？

<select1>A. 1 行</select1>
<select2>B. 2 行</select2>
<select3>C. 3 行</select3>
<select4>D. 4 行</select4>
<ans>C</ans>
<detail>
每呼叫一次 `print()` 就會產生一行輸出，就算括號裡是空的也一樣——只是那一行內容是空白而已。

三次 `print()` 呼叫，分別輸出：`A`、空白行、`B`，總共 3 行。
</detail>
