執行下面這段程式：

<pre><code>print(6 & 3)</code></pre>

會印出什麼？(提示：`6` 是 `110`，`3` 是 `011`)

<select1>A. 7</select1>
<select2>B. 5</select2>
<select3>C. 2</select3>
<select4>D. 0</select4>
<ans>C</ans>
<detail>
把 `110` 和 `011` 每一位對齊做 AND(兩個都是 1 才是 1)：

<pre><code>  110
& 011
-----
  010</code></pre>

`010` 換算成 10 進位是 `2`。
</detail>
