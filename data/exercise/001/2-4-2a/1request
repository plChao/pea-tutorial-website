閱讀下面的程式碼：

<pre><code>for i in range(2, 8, 2):
    print(i)</code></pre>

請問輸出是？

<select1>A. 2 4 6(每個數字各一行)</select1>
<select2>B. 2 4 6 8(每個數字各一行)</select2>
<select3>C. 2 3 4 5 6 7(每個數字各一行)</select3>
<select4>D. 0 2 4 6(每個數字各一行)</select4>
<ans>A</ans>
<detail>
`range(2, 8, 2)` 從 2 開始、每次跳 2、**不包含** 8，所以會依序產生 2、4、6，一共三個數字。
</detail>
