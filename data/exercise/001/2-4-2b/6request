第一行讀入一個整數 target。接下來每行讀入一個猜測值，直到猜中 target，或是讀到 `-1`(代表放棄)為止：

- 如果猜中了，輸出「猜對了，共猜了 X 次」(X 是總共猜的次數，不含放棄的 -1)
- 如果讀到 -1 都還沒猜中，輸出「放棄」

範例：如果輸入是

<pre><code>7
3
5
7</code></pre>

輸出應該是：

<pre><code>猜對了，共猜了 3 次</code></pre>
