閱讀下面的程式碼：

<pre><code>rows = [[0] * 2] * 3
rows[1][0] = 9
print(rows)</code></pre>

請問輸出是？

<select1>A. [[0, 0], [9, 0], [0, 0]]</select1>
<select2>B. [[9, 0], [9, 0], [9, 0]]</select2>
<select3>C. [[0, 0], [0, 0], [0, 0]]</select3>
<select4>D. 程式會產生錯誤</select4>
<ans>B</ans>
<detail>
`[[0] * 2] * 3` 是把「同一個」`[0, 0]` 串列的參考複製了 3 次，`rows` 裡的三列其實都指向同一個物件。所以只改 `rows[1][0]`，三列會一起變成 `[9, 0]`，結果是 `[[9, 0], [9, 0], [9, 0]]`。
</detail>
