執行以下程式：

<pre><code>print("x", "y", "z", sep="-")</code></pre>

輸出會是什麼？

<select1>A. x y z</select1>
<select2>B. x-y-z</select2>
<select3>C. x, y, z</select3>
<select4>D. xyz</select4>
<ans>B</ans>
<detail>
`sep="-"` 把 `print()` 原本用來分隔多個值的空白，改成 `-`，所以三個值中間都會用 `-` 隔開，輸出 `x-y-z`。
</detail>
