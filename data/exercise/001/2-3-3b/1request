執行下面這段程式：

<pre><code>print(5 << 2)</code></pre>

會印出什麼？

<select1>A. 10</select1>
<select2>B. 20</select2>
<select3>C. 7</select3>
<select4>D. 25</select4>
<ans>B</ans>
<detail>
左移 n 位等於乘以 `2 ** n`。`5 << 2` 就是 `5 * (2 ** 2) = 5 * 4 = 20`。
</detail>
