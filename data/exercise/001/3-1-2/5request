凱薩密碼(Caesar cipher)是一種簡單的加密方式：把字串裡每個字母，往後位移固定的位數，超過 `'z'` 就繞回 `'a'` 重新算。

輸入兩行：第一行是一個全部為小寫英文字母的字串，第二行是位移量 `k`(整數)。請輸出加密後的字串。

範例：如果輸入是

<pre><code>abcxyz
3</code></pre>

輸出應該是：

<pre><code>defabc</code></pre>

(`a` 往後 3 位是 `d`；`x` 往後 3 位超過 `z`，繞回去變成 `a`)
