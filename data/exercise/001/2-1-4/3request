閱讀下面的程式碼：

<pre><code>nums = [5, 10, 15]
nums.append(20)
nums.insert(0, 1)
nums.remove(10)
print(nums)</code></pre>

請問輸出是？

<select1>A. [1, 5, 15, 20]</select1>
<select2>B. [5, 15, 20, 1]</select2>
<select3>C. [1, 5, 10, 15, 20]</select3>
<select4>D. [1, 5, 20]</select4>
<ans>A</ans>
<detail>
依序執行：`append(20)` → `[5, 10, 15, 20]`；`insert(0, 1)` 在索引 0 插入 1 → `[1, 5, 10, 15, 20]`；`remove(10)` 刪除值為 10 的元素 → `[1, 5, 15, 20]`。
</detail>
