讀入一個 0~100 的整數分數 score，依照下面規則輸出對應的中文等第(每筆資料只會符合一種情況)：

- score >= 90 → 優
- 80 <= score < 90 → 甲
- 60 <= score < 80 → 及格
- score < 60 → 不及格

範例：如果輸入是

<pre><code>85</code></pre>

輸出應該是：

<pre><code>甲</code></pre>
