這一題改編自 APCS 2016 年 10 月的實作題「三角形辨別」，資料範圍縮小、輸出改成中文，讓你可以只用今天學的 `if/elif/else` 就完成。

依序讀入三個正整數(三行，每行一個)，代表三條線段的長度。請判斷：

1. 這三條線段能不能構成一個三角形：任兩邊之和要大於最長邊，才能構成三角形。
2. 如果可以構成三角形，再依照最長邊的平方，跟另外兩邊平方和的大小關係，判斷是哪一種三角形：
   - 兩邊平方和 < 最長邊平方 → 鈍角三角形
   - 兩邊平方和 = 最長邊平方 → 直角三角形
   - 兩邊平方和 > 最長邊平方 → 銳角三角形

輸出兩行：第一行把三個數字由小到大印出(用空格隔開)；第二行輸出判斷結果(「不是三角形」、「鈍角三角形」、「直角三角形」、「銳角三角形」其中一種)。

範例：如果輸入是

<pre><code>3
4
5</code></pre>

輸出應該是：

<pre><code>3 4 5
直角三角形</code></pre>

再舉一個例子，如果輸入是

<pre><code>10
10
100</code></pre>

輸出應該是：

<pre><code>10 10 100
不是三角形</code></pre>

<p class="placeholder-note">提示：還沒學到串列(list)排序，用第 2-4-1 章教的「兩兩比較 + 交換」方式，把三個變數排成由小到大即可。</p>
