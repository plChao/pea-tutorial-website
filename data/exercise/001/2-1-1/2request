任務 2：請利用迴圈跟 print() 的 end 參數，在「同一行」依序印出 1 到 5，數字之間用一個空白分開，結尾不要換行。

範例輸出：

```
1 2 3 4 5
```
---HINT---
print() 預設每次呼叫都會自動換行（end="\n"）。想讓多次 print() 接在同一行，可以把 end 改成別的字串，例如 end=" "：

```
for i in range(1, 6):
    print(i, end=" ")
```
---STDIN---
