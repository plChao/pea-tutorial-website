# ZeroJudge 延伸練習推薦

針對課程三個進度點，各推薦適合難度的 ZeroJudge 題目。

> ⚠️ zerojudge.tw 是 JS 動態渲染網站，題目敘述無法直接抓取核對，以下是交叉比對多個第三方解題筆記整理而成。**指派給學生前建議自行上 zerojudge.tw 開一次題確認完整敘述、範例測資、以及是否要讀到 EOF。**

## ① 完成 2-1-1b(＋緊接的 2-1-2 輸入)之後 — 純輸出/輸入基礎

此時只學過 `print()`／`end`／`input()`／`int()`／`.split()`，還沒教 if 或正式的算術運算子章節。

1. **a001. 哈囉** — 讀一個名字，印出打招呼訊息。純 `input()` + `print()`，零運算，最適合當第一題實戰。
2. **a002. 簡易加法** — 讀兩個整數 a、b，印出 a+b。只用到直覺的 `+`。

（只找到這兩題有把握、且不需要 if/迴圈；再往後的 a003「兩光法師占卜術」、a004「文文的求婚」都要 if/elif，得留到 2-3-1 之後。）

## ② 完成 2-2-3b 之後 — 位元運算

此時已學過算術/比較/邏輯/位元運算(含位移、`bin()`、`(n>>k)&1`)，但還沒教 if/迴圈。

1. **a034. 二進位制轉換** — 十進位轉二進位。可以直接用課程剛教過的 `bin(n)[2:]` 一行解完，跟 2-2-3a 教材內容直接呼應，不需要迴圈。

⚠️ 只找到這一題信心足夠高、確定不需要迴圈的位元題。另外查到 **a414. 位元運算之進位篇**(數某數 +1 要進位幾次＝從右邊數連續幾個 1)概念上很切題，但參考解法都需要迴圈逐位檢查——挪到下面第③組，當「位元+while」複習題。

## ③ 完成 2-4-2 之後 — while 迴圈

此時全部基礎語法都教過了。

1. **a024. 最大公因數(GCD)** — 輾轉相除法，while 迴圈最經典的入門題，直接對應教材「不知道要跑幾次」的情境。
2. **a038. 數字翻轉** — 用 `%10`／`//10` 逐位反轉數字，是算術(2-2-1)+while(2-4-2)的自然綜合題。⚠️ 查到的參考解法用 `while True: try/except` 讀到 EOF 處理多筆資料——這手法課程沒教過，指派時可能要提醒學生一句，或老師先給個程式碼片段帶過去。
3. **a414. 位元運算之進位篇** — 位元運算+while 的綜合複習題，把 2-2-3a/b 學過的位元技巧跟剛學的 while 結合起來用。

## 待辦(尚未執行)

如果之後想把這些連結直接加進對應章節文件(`2-1-1b.html`、`2-2-3b.html`、`2-4-2.html`)末尾當「延伸練習」區塊，跟我說一聲即可。
